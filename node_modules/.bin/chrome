#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_chrome@0.1.0@chrome/index.js" "$@"
  ret=$?
else 
  node  "$basedir/../_chrome@0.1.0@chrome/index.js" "$@"
  ret=$?
fi
exit $ret
